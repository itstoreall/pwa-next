import Container from '@/components/Container';

const Account = () => {
  return (
    <div>
      <main>
        <Container label={'page'}>
          <span>Account</span>
        </Container>
      </main>
    </div>
  );
};

export default Account;
