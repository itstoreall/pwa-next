import Container from '@/components/Container';

const Home = () => {
  return (
    <div>
      <main>
        <Container label={'page'}>
          <span>Home</span>
        </Container>
      </main>
    </div>
  );
};

export default Home;
